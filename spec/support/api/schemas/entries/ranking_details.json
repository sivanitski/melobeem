{
  "type": "object",
  "required": ["entry"],
  "properties": {
    "entry": {
      "type": "object",
      "required": ["id", "total_votes", "rank", "level"],
      "properties": {
        "id": {"type": "integer"},
        "total_votes": {"type": "integer"},
        "rank": {"type": "integer"},
        "level": {"type": "integer"}
      }
    }
  }
}
