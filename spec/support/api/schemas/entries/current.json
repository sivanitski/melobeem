{
  "type": "object",
  "required": ["entry"],
  "properties": {
    "entry": {
      "type": "object",
      "required": ["id", "image_url", "name", "level", "total_votes", "rank"],
      "properties": {
        "id": {"type": "integer"},
        "image_url": {"type": "string"},
        "name": {"type": "string"},
        "level": {"type": "integer"},
        "total_votes": {"type": "integer"},
        "rank": {"type": "integer"}
      }
    }
  }
}
